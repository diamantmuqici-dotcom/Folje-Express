# Required for the private admin + Vercel Blob storage
ADMIN_PASSWORD=replace-with-a-strong-private-password

# Vercel Blob storage credential if your store uses static-token auth.
BLOB_READ_WRITE_TOKEN=replace-with-vercel-blob-token
